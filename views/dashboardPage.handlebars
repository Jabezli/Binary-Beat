<div class="text-center">
    <h1 class="text-center">See Your Post Below</h1>
    {{#each posts}}

        <div class="card text-center">
            <div class="card-body">
                <h5 class="card-title">{{title}}</h5>
                <p class="card-text">{{body}}</p>
                <p class="card-text">Posted at {{format_date created_at}}</p>
                <a href="dashboard/post/{{id}}" class="btn btn-primary">Edit Post</a>
                <button class="btn btn-primary deleteBtn" postId="{{id}}">Delete Post</button>
        {{!-- <div>
        
        <button type="button" class="mt-3 btn btn-light editPost"><a href="/dashboard/post/{{id}}" class="link-underline-opacity-0 link-dark" >Edit</a></button>
      
        <button type="button" class="mt-3 btn btn-light deletePost">Delete Post</button>
        </div> --}}
            </div>
        </div>

    {{/each}}

</div>

<div class="text-center">
    <a href="/dashboard/createPost" class="btn btn-primary">Create a New Post</a>
</div>

<script src="/js/dashboard.js"></script>